# OPERATION LOG 開発ルール

## 基本ルール
- 修正は最小限・ピンポイントで行う
- 広範囲な変更は禁止（バグの原因になる）
- 必ず承認を得てから実装する
- 先祖返り（古いベースファイルの使用）禁止

## バージョン管理
- V2版・Lo版・SW（service-worker.js）は常にセットで同じバージョン番号にする
- バージョンはファイル先頭のコメントブロックで管理する

## UI禁止事項
- EBボタンの位置を移動しない
- ライト/ダークトグルボタンを移動しない
- 大幅なUI変更は事前確認必須

## 用語ルール
- 運用地（QTHは使わない）
- ウィンドウ（モーダルは使わない）
- 候補（サジェストは使わない）

## Lo版の制限
- Lo版にGAS関連機能は追加しない

## Git
- .envはコミットしない
